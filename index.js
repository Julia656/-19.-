// // const numbers = [5, 8, -1, '-6', 11, 'ololo', '-2'];
// // let sum = 0;
// for (let i = 0; i < numbers.length; i++) {
//   sum += numbers[i];
// }
// alert('Sum is ' + sum);


// Оголошення масиву

// numbers = [5, 8, -1, '-6', 11, 'ololo', '-2']

// Масив містить числа, рядки з числами ('-6', '-2') і некоректний рядок ('ololo').

// Оголошення змінної sum

// sum = 0

// Запуск циклу

// for (let i = 0; i < numbers.length; i++)

// Масив містить 7 елементів, тому i буде змінюватися від 0 до 6.

// Ітерації циклу:

// i = 0

// sum += numbers[0] → sum = 0 + 5 = 5

// i = 1

// sum += numbers[1] → sum = 5 + 8 = 13

// i = 2

// sum += numbers[2] → sum = 13 + (-1) = 12

// i = 3

// sum += numbers[3] → sum = 12 + '-6'

// '6' — це рядок, оператор + при складанні числа і рядка перетворює число на рядок:

// sum = '12-6'

// Тепер sum стає рядком!

// i = 4

// sum += numbers[4] → '12-611'

// '12-6' + 11 дає рядок '12-611', а не математичний результат.

// i = 5

// sum += numbers[5] → '12-611ololo'

// '12-611' + 'ololo' просто об'єднує рядки.

// i = 6

// sum += numbers[6] → '12-611ololo-2'

// '12-611ololo' + '-2' = '12-611ololo-2'









// Помилка сталася на ітерації i = 3, коли в sum додався рядок '-6', що призвело до конкатенації (об'єднання) рядків.



















//   const numbers = [5, 8, -1, '-6', 11, 'ololo', '-2'];
// let sum = 0;

// for (let i = 0; i < numbers.length; i++) {
//   let num = Number(numbers[i]); 
//   if (!isNaN(num)) { 
//     sum += num;
//   }
// }

// alert('Sum is ' + sum); 


















